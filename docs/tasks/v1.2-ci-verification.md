# V1.2 CI 修复与正式构建验收

2026-09-20 用户确认按评估方案实施，并延续 V1.2 提交、PR、部署授权。基线为 `3f3dd86`，工作树干净。

## 范围

- 修复 `.playwright/` 隐藏目录未被上传的问题，仅上传测试诊断目录，缺失产物须告警。
- Playwright 使用当前源码新构建的静态产物，通过独占 preview 服务运行；本地和 CI 测同一入口，禁止复用其他窗口的 dev 服务。
- 先保留 Linux WebKit 原失败断言，补充输入时间、实际几何和透明度采样证据；区分动画状态与测试时序。
- 定位并修复长按释放后的运动，以及第三页交接丢失中间帧；不通过跳过、增加重试或放宽断言获得通过。
- 完整门禁通过后更新 PR #2、验证远端 Quality，再合并和部署 V1.2。

## 已知证据

- 本地 249/249；Linux CI run 35507604945 为 246/249，失败在 Chromium 首用例 navigation context destroyed、WebKit 过渡采样、WebKit 长按释放 113.72px。
- artifact 步骤日志显示 `include-hidden-files: false` 且 `No files were found`；GitHub artifact API 返回 0。
- CI 实际仅 2 workers，当前不以进一步降低并发作为无证据修复。
- 开发服务器可能在冷启动触发重载。正式静态产物不需要开发热更新；这也是上线前更合适的验收对象。
- 回滚基线 `v1.1.0` 保留，生产异常通过新 revert commit 恢复，不改写历史。

## 验证

先运行相关三浏览器专项，再运行完整 `npm run verify`、critical audit 与 diff check。远端失败必须能下载诊断产物，记录其帧/输入数据。修改覆盖 package/scripts、Playwright、Quality YAML、受影响的动效与测试；不改字体、内容、视觉方向、仓库权限或分支保护。

## 结果

实施中。
