# V1.4 发布整合

2026-09-21 用户明确授权按 GitHub Issue → 关联 PR → 质量门禁 → 合并 main → 部署 → 版本归档的流程发布。需求与验收清单：[Issue #4](https://github.com/hebi-Chinese/hebi-Chinese.github.io/issues/4)。基线为 v1.3.0 / 18a3237。

## 更新内容

- About 的手机图文交错编排，保留六条原文和连线，调整可读字号与留白。
- 手机项目卡片居中展示、邻卡露边、原生吸附滚动和首尾循环。连续输入、反向切换、尺寸变化及非活动卡片隔离作为其交互验收。
- Blog、Likes、Now 收紧手机布局，调整图形位置；隔离首二屏层叠上下文，避免遮挡后续内容。

桌面轮盘、首屏、产品展示、作者内容、项目目录与路由不改。760px 切换点由 CSS 决定，移动运行时从真实几何判断可见性，不重复维护断点。移动卡片沿用项目稳定 slug 和共享目录；实例拥有事件、ResizeObserver 和结束计时器，销毁恢复静态链接。没有新增依赖或页面级状态。

## 发布检查

先建立 Issue，再更新 package/lock 为 1.4.0；完整检查当前源码，不用历史 357 项通过代替本轮验收。执行 npm run verify、生产依赖审计、diff check，并实际查看手机/平板/桌面页面与操作。保留零重试和现有 Linux Firefox Xvfb 验证。

PR 使用 Closes #4，当前候选的 Linux Quality 通过后才合并，合并自动关闭 Issue。Pages 对合并提交再次验收，部署后核对九个入口及资源，验证移动卡片后建立 v1.4.0 标签/Release，并回填 Issue、PR 和本地记录。

已有 mobile-v1.4.md / mobile-audit-fixes.md 的“未提交发布”描述属于原实现任务的历史状态，本轮按最新用户授权整合。浏览器仿真不等于实体 iOS/Android/微信验证，不虚报真机完成。保留 v1.3.0 回滚基线，回滚使用新 revert commit，不强推或移动标签。暂存时排除原型、测试报告、缓存和 .codex。

## 验收结果

- 本轮 npm run verify：62 文件零错误/警告/提示；9 入口、13 图片构建成功；Chromium/Firefox/WebKit 357/357（3.1m），零重试。
- npm audit --omit=dev --audit-level=critical：0 vulnerabilities；git diff --cached --check：通过。package/lock 只改根版本 1.4.0，没有依赖变动。
- 首页 JS gzip：内联 6016 bytes，外部 11075 bytes，总计 17091 bytes，低于 25 kB 预算。
- 实际查看 390px About/项目卡/Likes/Now、768px About、1440px 项目轮盘，排版与内容可读，控制台零错误/警告。Chromium 原生触摸输入模拟完成 01→05→01，居中且没有打开额外页面；它不是实体手机测试。
- 完整差异复核：未发现待处理 P0/P1/P2/P3。新移动组件及 runtime 均不足 300 行，具名处理函数不超 50 行；ProjectsWheel 原有深模块只增加组合入口和局部显示规则，未扩充桌面状态机。路由、目录与作者内容未变，未发现待提交凭据或浏览器产物。
- 本轮没有改用户已确认的产品实现，只进行版本和发布记录整合。PR/Linux/部署结果在 Issue #4 与关联 PR/Release 回填；本地验收不能替代远端放行。

## 已批准的远端键盘焦点修复

首轮 Linux run 35604257079 为 356/357。WebKit 快速键盘测试结束时活动卡片失焦，截图中项目标题持有焦点；01 状态断言曾瞬时通过，之后活动卡为 05。本地单项诊断性重复 8/8 通过，未用该结果绕过远端失败。用户已明确授权“修复并继续发布”。

先用 Playwright Clock 暂停初始绘制帧，在真实首页书签落点开始卡片键盘操作，再恢复帧，验证导航不得覆盖用户焦点。优先修正初始锚点导航与键盘输入竞争，不更改排版、卡片数据或运动效果；保留原快速切换断言，补充稳定回归后重跑完整门禁。资料：https://playwright.dev/docs/clock （时间是测试边界）；https://developer.mozilla.org/en-US/docs/Web/API/Element/keydown_event （键盘事件从实际焦点元素冒泡）。

已取得稳定红绿证据：新增 initial bookmark landing 测试在旧源码 WebKit 上失败于活动卡片失焦（5s），与 Linux trace 的标题抢焦点一致。原全局导航只将垂直方向等键视为用户中断，遗漏了卡片的左右方向键；用户已操作后，初始化双 rAF 仍聚焦分区标题。仅将 ArrowLeft/ArrowRight 加入现有中断键表，取消待执行聚焦并标记已交互。重建后新增回归三浏览器 3/3（5.7s）通过，没有改卡片状态机、布局、原断言、等待阈值或重试数。

修复后移动端与分区导航专项 63/63（44.9s），含原失败的自然时间快速输入测试；npm run check 62 文件零诊断，构建与生产依赖审计通过，diff check 通过。新增一项三浏览器回归后，全量为 360 项；完整本地与最新候选 Linux 门禁随后执行。
