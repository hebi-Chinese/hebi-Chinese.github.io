---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Nav from '../../components/Nav.astro';
import ScrollReveal from '../../components/ScrollReveal.astro';
import { getCollection } from 'astro:content';
import '../../styles/global.css';

const essays = (await getCollection('essays', ({ data }) => !data.draft))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
---
<BaseLayout title="何必 / essays">
  <Nav />
  <main style="padding-top: 6rem; min-height: 100vh;">
    <div class="wrap">
      <div class="section-label" data-reveal>Essays</div>
      <h2 class="section-title" data-reveal style="margin-bottom: 3rem;">长文</h2>

      {essays.length === 0 ? (
        <div data-reveal style="padding: 3rem 0; text-align: center;">
          <p style="font-family: 'Instrument Serif', serif; font-size: 1.3rem; color: var(--text-muted); line-height: 2;">
            还没有长文。<br/>写不动就不写。
          </p>
        </div>
      ) : (
        <div style="max-width: 42rem;">
          {essays.map(essay => (
            <a href={`/essays/${essay.slug}`} style="display: block; padding: 1.5rem 0; border-bottom: 1px solid var(--surface-2);">
              <time style="font-family: 'JetBrains Mono', monospace; font-size: 0.6rem; color: var(--text-muted); letter-spacing: 0.1em;">
                {essay.data.date.toLocaleDateString('zh-CN', { year: 'numeric', month: '2-digit', day: '2-digit' })}
              </time>
              <h3 style="font-family: 'Space Grotesk', sans-serif; font-size: 1.05rem; font-weight: 500; color: var(--text-secondary); margin-top: 0.4rem;">
                {essay.data.title}
              </h3>
              {essay.data.description && (
                <p style="font-size: var(--text-small); color: var(--text-muted); margin-top: 0.3rem;">{essay.data.description}</p>
              )}
            </a>
          ))}
        </div>
      )}
    </div>
  </main>
  <ScrollReveal />
</BaseLayout>
